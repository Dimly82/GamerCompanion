<Window x:Class="GamerCompanion.Views.RegisteredGamesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Registered Apps" Height="400" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
            <ComboBox x:Name="ProcessComboBox" Width="180" Margin="0,0,5,0"/>
            <TextBox x:Name="AppNameBox" Width="120" Margin="0,0,5,0"
                    ui:ControlHelper.PlaceholderText="App name"/>
            <Button Content="Add" Click="Add_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5,0,5">
            <Button Content="Remove selected" Click="Remove_Click"/>
        </StackPanel>

        <ListBox Grid.Row="2" x:Name="AppsList" ItemsSource="{Binding Apps}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" Width="150" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding ProcessName}" Width="130" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>