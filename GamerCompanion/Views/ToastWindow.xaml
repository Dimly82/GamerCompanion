<Window x:Class="GamerCompanion.Views.ToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        Width="300" Height="80">
    <Border Background="#222222" CornerRadius="8"
            Opacity="0.95">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="ToastText"
               Foreground="White"
               FontSize="14"
               TextWrapping="Wrap"
               VerticalAlignment="Center"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" >
                <Button Content="Remind in 5 min" Margin="0,5,5,0" Click="RemindLater_Click" Foreground="White" Background="#444444"/>
                <Button Content="Dismiss" Margin="0,5,5,0" Click="Dismiss_Click" Foreground="White" Background="#444444"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>